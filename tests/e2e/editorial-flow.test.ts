import { describe, expect, it } from "vitest";
import { MockOpenAIAdapter } from "@/src/adapters/openai";
import { MockNotionAdapter } from "@/src/adapters/notion";
import { decideDemoApproval, executeDemoPipeline } from "@/src/services/demo-pipeline";
describe("E2E tendencia → aprobación", () => { it("recorre todas las etapas sin red ni costo", async () => { const result = await executeDemoPipeline({ title: "Servicio ficticio para Guatemala", description: "Fixture E2E", sources: [{ id: "primary", title: "Oficial", url: "https://official.example/item", domain: "official.example", excerpt: "El servicio cambia a las 08:00.", isPrimary: true }, { id: "secondary", title: "Verificación", url: "https://media.example/item", domain: "media.example", excerpt: "La segunda fuente confirma el horario.", isPrimary: false }], profiles: [], score: { velocity: 85, recurrence: 75, sourceQuality: 90, guatemalaRelevance: 100, evidence: { e2e: true } } }, { openai: new MockOpenAIAdapter(), notion: new MockNotionAdapter() }); expect(result.history).toEqual(["DETECTED", "SCORING", "SCORED", "INVESTIGATING", "EVIDENCE_READY", "GENERATING_VARIANTS", "DRAFTS_READY", "SYNCING_NOTION", "AWAITING_APPROVAL"]); expect(decideDemoApproval({ status: result.history.at(-1)!, action: "APPROVE", role: "EDITOR_SENIOR", risk: result.risk!, comment: "E2E revisado", confirmed: true })).toBe("APPROVED"); }); });
