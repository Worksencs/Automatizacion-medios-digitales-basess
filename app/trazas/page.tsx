import { AppFrame, EmptyState } from "@/components/app-frame";
import { prisma } from "@/src/db";
export const dynamic = "force-dynamic";
export default async function TracesPage() { let traces: any[] = []; try { traces = await prisma.agentTrace.findMany({ include: { workflowRun: { include: { trend: true } } }, orderBy: { createdAt: "desc" }, take: 100 }); } catch {} return <AppFrame active="/trazas" title="Trazabilidad" subtitle="Decisiones, herramientas, modelos, duración, reintentos y fuentes"><article className="panel data-panel">{traces.length ? <table><thead><tr><th>Flujo</th><th>Agente</th><th>Modelo</th><th>Prompt</th><th>Duración</th><th>Estado</th></tr></thead><tbody>{traces.map((trace) => <tr key={trace.id}><td><a href={`/expedientes/${trace.workflowRun.trendId}`}>{trace.workflowRun.trend.title}</a><small>{new Intl.DateTimeFormat("es-GT", { dateStyle: "short", timeStyle: "short", timeZone: "America/Guatemala" }).format(trace.createdAt)}</small></td><td>{trace.agent}</td><td>{trace.model}</td><td>{trace.promptVersion}</td><td>{trace.durationMs} ms</td><td><span className="badge badge-green">{trace.finalStatus}</span></td></tr>)}</tbody></table> : <EmptyState title="No hay trazas todavía" detail="Las ejecuciones del worker aparecerán aquí sin secretos ni credenciales." />}</article></AppFrame>; }
