"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { AppFrame } from "@/components/app-frame";

export default function NewTrendPage() { const [message, setMessage] = useState(""); const router = useRouter(); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setMessage("Guardando…"); const form = new FormData(event.currentTarget); const payload = Object.fromEntries(form); const response = await fetch("/api/trends", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); const result = await response.json(); if (!response.ok) return setMessage(result.error ?? "No se pudo guardar"); router.push(`/expedientes/${result.trend.id}`); }
  return <AppFrame active="/radar" title="Nueva tendencia" subtitle="Registra una señal manual; el worker realizará la puntuación y la investigación"><form className="form-card" onSubmit={submit}><label>Título del tema<input name="title" required minLength={5} maxLength={240}/></label><label>Descripción inicial<textarea name="description" rows={4} maxLength={4000}/></label><div className="form-grid"><label>URL de origen<input name="originUrl" type="url" placeholder="https://…"/></label><label>Fuente<input name="sourceName" maxLength={160}/></label><label>Fecha de publicación<input name="publishedAt" type="datetime-local"/></label><label>Alcance o señal observada<input name="observedReach" type="number" min="0"/></label><label>Medio sugerido<select name="suggestedOutletSlug"><option value="">Por definir</option><option value="insonimio-guatemala">Insonimio Guatemala</option><option value="yo-amo-guate">Yo Amo Guate</option><option value="trece-noticias">Trece Noticias</option><option value="tv-azteca-guate">TV Azteca Guate</option></select></label></div><label>Comentario del reportero<textarea name="userComment" rows={3}/></label><div className="form-actions"><a href="/radar">Cancelar</a><button className="primary-button" type="submit">Guardar e iniciar flujo</button></div>{message && <p className="form-message" role="status">{message}</p>}</form></AppFrame>; }
