import { prisma } from "@/src/db";
export const dynamic = "force-dynamic";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const query = await searchParams; let users: any[] = []; try { users = await prisma.user.findMany({ where: { active: true }, include: { role: true }, orderBy: { name: "asc" } }); } catch {} return <main className="login-page"><section className="login-card"><div className="brand login-brand"><span className="brand-mark">N</span><span><strong>Nexo</strong><small>Sala editorial IA</small></span></div><h1>Acceso interno</h1><p>Selecciona un usuario ficticio para evaluar los permisos del MVP.</p>{query.error && <p className="warning-box">No se pudo iniciar sesión.</p>}<form action="/api/auth/login" method="post"><label>Usuario<select name="email" required><option value="">Seleccionar…</option>{users.map((user) => <option value={user.email} key={user.id}>{user.name} · {user.role.code}</option>)}</select></label><button className="primary-button" type="submit">Ingresar al sistema</button></form><small>En modo real, reemplaza este selector por el proveedor de identidad interno de la organización.</small></section></main>; }
